USE [WorksSp]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[USP_WorksSp_User_GetByName] (@UserName VARCHAR(50))
AS
BEGIN
	SELECT TOP 1 * FROM [dbo].[WorksSp_User]
		WHERE UserName = @UserName;
	SET NOCOUNT ON;
END
GO
